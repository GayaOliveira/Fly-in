[project]
name = "fly_in"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic",
    "matplotlib",
    "customtkinter",
]

[dependency-groups]
dev = [
    "rich>=15.0.0",
]
